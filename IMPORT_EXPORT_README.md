# คู่มือ Logic นำเข้า (Import) และส่งออก (Export) ข้อมูลลูกค้า

---

## 1. Export (ส่งออก Excel)

**Endpoint**: `GET /api/customers/export/excel`  
**ไฟล์**: `routes/customers.js`

### ขั้นตอน
1. ดึงข้อมูลลูกค้าทั้งหมดจาก MongoDB พร้อม populate ชื่อโครงการ
2. สร้างไฟล์ `.xlsx` ด้วย ExcelJS
3. ส่งกลับเป็นไฟล์ดาวน์โหลด

### คอลัมน์ที่ Export

| คอลัมน์ | หัวข้อใน Excel | หมายเหตุ |
|---|---|---|
| No. | `No.` | ลำดับ auto-increment |
| เบอร์โทร | `ເບີໂທ` | format text (`@`) ป้องกัน Excel แปลงเป็นตัวเลข |
| ชื่อ | `ຊື່ ແລະ ນາມສະກຸນ` | |
| ประเภทลูกค้า | `ປະເພດ` | แปลง value key → label ภาษาไทย |
| โครงการ | `ໂຄງການ` | หลายโครงการคั่นด้วย `, ` |
| พนักงาน | `ພະນັກງານ` | |
| หมายเหตุ | `ໝາຍເຫດ` | |
| วันที่ติดต่อ | `ວັນທີທັກ` | format `DD/MM/YYYY HH:MM` |

---

## 2. Import (นำเข้า Excel) — มี 2 เส้นทาง

---

### 🟢 Path A: Frontend-parsed (ใช้งานอยู่จริง)

**Component**: `src/components/ImportExcelDialog.vue`  
**API ที่เรียก**: `POST /api/customers` (ทีละแถว)

#### ขั้นตอนโดยละเอียด

```
1. โหลดไฟล์ .xlsx ในเบราว์เซอร์ด้วย ExcelJS (ไม่อัปโหลดไฟล์ไปเซิร์ฟเวอร์)
2. อ่านแถวหัวข้อ (Row 1) → สร้าง colMap (ชื่อคอลัมน์ → หมายเลขคอลัมน์)
3. Auto-create ประเภทลูกค้าที่ยังไม่มีในระบบ
4. Auto-create โครงการที่ยังไม่มีในระบบ
5. Parse แต่ละแถว → สร้าง row object
6. วนลูป: เรียก POST /api/customers สำหรับแต่ละแถว (sequential)
7. แสดงผล: เพิ่มใหม่ N รายการ, อัปเดต N รายการ, ผิดพลาด N รายการ
```

#### หัวข้อคอลัมน์ที่รองรับ (Import Path A)

| ข้อมูล | หัวข้อที่รองรับ |
|---|---|
| เบอร์โทร | `ເບີໂທ`, `phone` |
| ชื่อ | หัวข้อที่มีคำว่า `ຊື່` หรือ `name` |
| ประเภท | `ປະເພດ`, `ປະເພດລູກຄ້າ`, `type`, `customer type` |
| โครงการ | `ໂຄງການ`, `project` (รองรับหลายโครงการคั่นด้วย `,`) |
| พนักงาน | `ພະນັກງານ`, `ພະນັກງານດູແລ`, `staff` |
| หมายเหตุ | `ໝາຍເຫດ`, `ລາຍລະອຽດ`, `notes` |
| วันที่ | `ວັນທີທີ່ລູກຄ້າທັກມາ`, `date` |

#### พิเศษ
- **ประเภทลูกค้า**: รองรับหลายประเภทคั่นด้วย `,`
- **โครงการ**: รองรับหลายโครงการคั่นด้วย `,`
- **วันที่**: รองรับ Excel date serial, JavaScript Date, และ string

---

### 🟡 Path B: Backend-parsed (มีอยู่แต่ไม่ได้ใช้งาน)

**Endpoint**: `POST /api/customers/import/excel`  
**ไฟล์**: `routes/customers.js`

> ⚠️ Path นี้มีอยู่ในโค้ดแต่ **`ImportExcelDialog.vue` ไม่ได้เรียกใช้** — ใช้ Path A แทน

#### ความแตกต่างจาก Path A

| จุด | Path A (Frontend) | Path B (Backend) |
|---|---|---|
| Parse Excel | ในเบราว์เซอร์ | ในเซิร์ฟเวอร์ (อัปโหลดไฟล์) |
| ประเภทลูกค้า | หลายประเภทต่อแถว | 1 ประเภทต่อแถว |
| โครงการ | หลายโครงการต่อแถว | 1 โครงการต่อแถว |
| Auto-create ประเภท | ✅ มี | ❌ ไม่มี |
| Response | created/updated count | count เท่านั้น |
| Header matching | strict / includes | regex flexible |

---

## 3. upsertCustomer() — ตรรกะ Upsert ด้วยเบอร์โทร

**ไฟล์**: `routes/customers.js` — ใช้ใน Path A ผ่าน `POST /customers`

### ขั้นตอน
```
1. ค้นหาลูกค้าด้วย phone: Customer.findOne({ phone })
2. ถ้าพบ → อัปเดตเฉพาะ field ที่กำหนด (findByIdAndUpdate + $set) → return 200
3. ถ้าไม่พบ → สร้างใหม่ (new Customer(data).save()) → return 201
```

### Field ที่อัปเดต vs รักษาเดิม

| Field | เมื่อ phone ซ้ำ |
|---|---|
| `name` | ✅ อัปเดต (ถ้ามีค่า) |
| `customerType` | ✅ อัปเดต (ถ้า array ไม่ว่าง) |
| `projects` | ✅ อัปเดต (ถ้า array ไม่ว่าง) |
| `contactedAt` | ✅ อัปเดต (ถ้ามีค่า) |
| `notes` | ✅ อัปเดต (ถ้าไม่ว่าง), รักษาเดิมถ้าว่าง |
| `ownerName / ownerLink / ownerPhoto` | ✅ อัปเดต |
| `updatedBy` | ✅ อัปเดต |
| `assignedStaff` | 🔒 รักษาเดิมเสมอ |
| `images` | 🔒 รักษาเดิมเสมอ |
| `contactLog` | 🔒 รักษาเดิมเสมอ |
| `no` (ลำดับ) | 🔒 รักษาเดิมเสมอ |

### ตัวอย่าง
```
รอบที่ 1: เบอร์ 020-1234-5678 ชื่อ "ທ้าວ ກໍ" → สร้างใหม่ (201)
รอบที่ 2: เบอร์ 020-1234-5678 ชื่อ "ທ้าວ ຂໍ" → อัปเดตชื่อ (200), staff/images คงเดิม
```

---

## 4. ข้อควรระวัง

- **เบอร์โทรต้องตรงกันทุกตัวอักษร** (รวม 0 นำหน้า) — ถ้าไม่ตรงจะสร้างรายการใหม่แทนอัปเดต
- **Export ไฟล์ Excel แล้ว Import กลับ**: หัวข้อคอลัมน์จาก Export จะถูก Import รับได้ (ชื่อคอลัมน์ `ຊື่ ແລະ ນາມສະກຸນ` match ได้เพราะใช้ `includes('ຊື่')`)
- **เบอร์โทรใน Excel**: ถูก format เป็น text (`@`) ป้องกัน Excel แปลงเป็นตัวเลข (0 หายไป)
